[package]
edition = "2021"
name    = "j2rs_query_string"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = "1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
